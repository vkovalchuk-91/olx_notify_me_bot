from django.apps import AppConfig


class InstaMonitorConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.insta_monitor'
    verbose_name = 'Instagram Monitor'
